using "io"

function fib int -> int do
  if dup 2 < do
    ret
  else
     dup 1 - fib swap 2 - fib + ret
  .
.

function main do
  "Getting the 30th number on the Fibonacci sequence\n" puts
  "832040  " puts 30 fib print
.
